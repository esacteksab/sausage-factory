# vim: syntax=yaml expandtab shiftwidth=2 softtabstop=2 autoindent
---
# A Test. ~/.tf-pr wins
binary: true
planfile: "tfplan.out"
mdfile: "tfplan.md"
#no-color: true # - The absence of this == false, so colorized input will exist.

## Proposed Layout Eventually
# repo / root
gh-tf-pr:
  # out files
  outfile:
    # file created by gh tf-pr create plan
    # file created by $binary plan -out outfile.plan
    plan: "plan.out"
    # file created by gh tf-pr create markdown
    md: "plan.md" 
  no-color: true
